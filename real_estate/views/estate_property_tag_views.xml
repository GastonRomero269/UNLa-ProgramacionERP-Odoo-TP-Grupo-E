<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Formulario de vista de etiquetas de propiedades -->
    <record id="estate_property_tag_view_list" model="ir.ui.view">
        <field name="name">estate.property.tag.view.list</field>
        <field name="model">estate.property.tag</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Etiquetas de Propiedad" editable="top">
                <field name="name" string="Nombre"/>
            </list>
        </field>
    </record>

    <!-- Formulario de accion de etiquetas de propiedades -->
    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Etiquetas de propiedad</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="estate_property_tag_view_list"/>
    </record>
    
</odoo>